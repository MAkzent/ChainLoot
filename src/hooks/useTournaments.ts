import { useContext } from 'react';

const useTournaments = () => {
  // const { tournaments, joinTournament } = useContext(Context);
  // return {
  //   tournaments,
  //   joinTournament,
  // };
};

export default useTournaments;
